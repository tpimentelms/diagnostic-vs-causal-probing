# Activate the uv-managed virtual environment.
#   First-time setup:  uv sync          # creates .venv from uv.lock
#   Then:              source activate.sh
# (Or skip activation entirely and run commands via:  uv run python main.py)
source .venv/bin/activate
